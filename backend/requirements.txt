# ── Core (Phase 1) ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
aiofiles>=24.1.0
python-dotenv>=1.0.1
apscheduler>=3.10.4

# ── Image (Phase 2) ───────────────────────────────────────────────────────────
Pillow>=11.0.0
pillow-heif>=0.21.0
# cairosvg requires Cairo system library:
#   Windows: install GTK3 runtime from https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer
#   Ubuntu:  apt-get install libcairo2-dev
#   macOS:   brew install cairo
cairosvg>=2.7.1

# ── Video / Audio (Phase 3) ───────────────────────────────────────────────────
# ffmpeg: install as system package (apt/brew/choco)
# yt-dlp: install with:  pip install yt-dlp

# ── Document (Phase 4) ────────────────────────────────────────────────────────
pdf2docx>=0.5.8
PyMuPDF>=1.24.14
WeasyPrint>=62.3
Markdown>=3.7
python-docx>=1.1.2

# ── Spreadsheet (Phase 4) ─────────────────────────────────────────────────────
openpyxl>=3.1.5
pandas>=2.2.3

# ── Archive (Phase 5) ─────────────────────────────────────────────────────────
py7zr>=0.22.0
patool>=3.0.0

# ── CAD (Phase 5) ─────────────────────────────────────────────────────────────
ezdxf>=1.3.4
matplotlib>=3.9.3
numpy-stl>=3.1.2

# ── Font (Phase 5) ────────────────────────────────────────────────────────────
fonttools>=4.55.0
brotli>=1.1.0

# ── Code & Data (Phase 5) ─────────────────────────────────────────────────────
PyYAML>=6.0.2
xmltodict>=0.14.2
